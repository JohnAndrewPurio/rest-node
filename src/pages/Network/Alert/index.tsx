import { IonAlert, IonButton } from "@ionic/react"

const Alert = () => {
    return (
        <IonAlert 
            isOpen
            header="Hello there"
        />   
    )
}

export default Alert
